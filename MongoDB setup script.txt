sudo apt install unzip
sudo chmod 600 Mongodb_instance_key.pem
ssh ubuntu@35.174.211.255 -i Mongodb_instance_key.pem
wget -c https://istd50043.s3-ap-southeast-1.amazonaws.com/meta_kindle_store.zip -O meta_kindle_store.zip
unzip meta_kindle_store.zip
rm -rf meta_kindle_store.zip

wget -qO - https://www.mongodb.org/static/pgp/server-4.4.asc | sudo apt-key add -
echo "deb [ arch=amd64,arm64 ] https://repo.mongodb.org/apt/ubuntu xenial/mongodb-org/4.4 multiverse" | sudo tee /etc/apt/sources.list.d/mongodb-org-4.4.list
sudo apt-get update
sudo apt-get install -y mongodb-org
sudo systemctl start mongod

sudo nano /etc/mongod.conf
sudo systemctl restart mongod

mongo
use kindle_review
db.createCollection("kindle_review")

mongoimport --db kindle_metadata --collection kindle_metadata --file meta_Kindle_Store.json --legacy
